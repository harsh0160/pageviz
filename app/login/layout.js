export const metadata = {
  title: 'Log in — Pageviz',
  description: 'Log in to Pageviz to see how your websites are doing: pageviews, top pages and where readers come from.',
}

export default function LoginLayout({ children }) {
  return children
}
