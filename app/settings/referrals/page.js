import { ReferralsView } from '../../_components/workspace/SettingsViews'

export const metadata = {
  title: 'Refer a friend — Pageviz',
}

export default function ReferralsPage() {
  return <ReferralsView />
}
