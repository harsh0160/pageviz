export const metadata = {
  title: 'Set a new password — Pageviz',
  description: 'Choose a new password for your Pageviz account.',
}

export default function ResetPasswordLayout({ children }) {
  return children
}
