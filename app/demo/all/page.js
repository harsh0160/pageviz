import OverviewView from '../../_components/workspace/OverviewView'

export const metadata = {
  title: 'All sites together — Pageviz',
}

export default function DemoOverview() {
  return <OverviewView />
}
