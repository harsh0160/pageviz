import { BillingView } from '../../../_components/workspace/SettingsViews'

export const metadata = {
  title: 'Plan & billing — Pageviz',
}

export default function DemoBillingPage() {
  return <BillingView />
}
