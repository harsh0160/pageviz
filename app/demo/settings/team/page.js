import { TeamView } from '../../../_components/workspace/SettingsViews'

export const metadata = {
  title: 'Team members — Pageviz',
}

export default function DemoTeamPage() {
  return <TeamView />
}
