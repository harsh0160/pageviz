import { AccountView } from '../../../_components/workspace/SettingsViews'

export const metadata = {
  title: 'Account — Pageviz',
}

export default function DemoAccountPage() {
  return <AccountView />
}
