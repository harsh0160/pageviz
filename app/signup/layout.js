export const metadata = {
  title: 'Sign up free — Pageviz',
  description: 'Create a free Pageviz account: cookieless website analytics for one site, no card required. Set up in about a minute.',
}

export default function SignupLayout({ children }) {
  return children
}
